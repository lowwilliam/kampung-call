{
  "name": "field-call-singapore",
  "version": "0.1.0",
  "private": true,
  "description": "A Singapore field-service training experience built with Three.js.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "validate": "node scripts/validate-project.mjs",
    "test": "npm run validate && npm run test:performance && npm run format:check",
    "test:performance": "node scripts/check-performance-budget.mjs",
    "format:check": "node scripts/check-format.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
