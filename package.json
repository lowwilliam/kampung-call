{
  "name": "kampung-call",
  "version": "0.1.0",
  "private": true,
  "description": "An island field-service training experience built with Three.js.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate": "node scripts/validate-project.mjs",
    "audit:world": "node scripts/audit-world.mjs",
    "test": "npm run validate && npm run audit:world && npm run test:performance && npm run format:check && npm run build",
    "test:performance": "node scripts/check-performance-budget.mjs",
    "format:check": "node scripts/check-format.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@gltf-transform/core": "^4.4.2",
    "vite": "^7.3.6"
  }
}
